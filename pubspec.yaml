name: shadow_clash
description: "A new Flutter project."

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.0.0  # For sprite and animation support
  provider: ^6.0.0  # If you're using state management
  cupertino_icons: ^1.0.8  # Ensure this is listed only once

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # For linting

flutter:
  uses-material-design: true

  # Add your assets (images and icons)
  assets:
    - assets/images/
    # - assets/icons/

  # Fonts section (uncomment and edit if you want custom fonts)
  # fonts:
  #   - family: RobotoMono
  #     fonts:
  #       - asset: fonts/RobotoMono-Regular.ttf
